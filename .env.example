# Environment
NODE_ENV=development

# Server
PORT=3000
API_VERSION=v1

# Database (PostgreSQL via Supabase)
# Format: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
# Get your connection string from: Supabase Dashboard > Settings > Database > Connection string > URI
DATABASE_URL=postgresql://postgres:soffob-paksyB-2sefce@db.oskvmpqzeitblfknswjq.supabase.co:5432/postgres

# JWT Authentication
# Generate a secure secret: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
JWT_EXPIRES_IN=7d

# CORS
# For production, set to your Railway frontend URL
# Comma-separated list of allowed origins, or * for all
CORS_ORIGIN=http://localhost:5173,http://localhost:3007

# Sentry Error Tracking (optional)
# Get your DSN from: https://sentry.io > Project Settings > Client Keys
SENTRY_DSN=
VITE_SENTRY_DSN=

# ============================================
# RAILWAY DEPLOYMENT NOTES
# ============================================
# Set these environment variables in Railway:
# - DATABASE_URL: Your Supabase connection string (use Transaction/Session pooler for serverless)
# - JWT_SECRET: Generate with: openssl rand -base64 64
# - NODE_ENV: production
# - CORS_ORIGIN: Your frontend URL (e.g., https://your-app.up.railway.app)
